// import { mensagem as msg, soma as total } from "./utils.js";
// console.log(msg());

import * as cmd from "./utils.js";
console.log(cmd.mensagem());

const numeros = [123,20,5,63,45];
console.log(cmd.soma(numeros));

console.log(cmd.maiorvalor(numeros));